// src/components/Preloader.tsx

import './Preloader.css'; // Optional styling

const Preloader = () => (
  <div className="preloader-overlay">
    <div className="spinner" />
  </div>
);

export default Preloader;
